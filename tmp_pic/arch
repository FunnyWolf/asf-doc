各种安全设备和云上的安全产品将日志通过 logstash/cribel/splunk hf等,云上可能是通过aliyun SLS aws s3 azure eventhub等将数据发送到SIEM平台(ELK/Splunk),然后SIEM中创建Rule,比如(Cloud-01-AWS-XXX EDR-01-HOST-XXX ) 然后通过action发送到外部,当前支持 webhook 发送到 ASP(agentic soc platform缩写) 提供的 Forwarder Plugin 或者ELK使用index action 然后ASP 使用 index_action 工具定时pull 存储alert的index 最终都是将数据发送到redis stack 的streams, streams中key就是 Cloud-01-AWS-XXX EDR-01-HOST-XXX . 后面都是在ASP中了,我就不强调了. ASP中有对应的Modules ,都是一个一个脚本,脚本名就叫 Cloud-01-AWS-XXX EDR-01-HOST-XXX 去对应队列取SIEM rule产生的告警,然后将告警 解析处理成三级数据结构,最上面的Case,是用户直接操作的,每个Case下面有很多Alert,每个alert可能包含多个artifact. Case/alert/artifact都能挂载enrichment用来存储一些补充信息 .ASP 的 Case支持用户讨论评论功能,用户可以在评论区加入评论. Case本身在UI上也支持状态流转(New In process close 等等)及各种Case管理功能. ASP 有 knowledge功能,用户可以将各种运营过程中的信息/知识/补充信息等加入到knowledge中. ASP 会在Case创建或者变动时(通常由Module代码直接调用状态变动) 自动对Case使用LLM进行分析,并给出分析报告,用户可以在界面中查看这个报告,llm分析会处理Case本身数据,case的讨论,case相关联的knowledge. ASP还支持Playbook框架,用户可以自定义Playbook对Case进行操作.当前已经内置了手动调用前面提到的自动分析(相当于手动触发一次),将已关闭的Case的所有信息提取成一条knowledge, 调用内置的TI接口将TI enrichment信息加入到Case的关联的alert关联的artifact中. Case还可以关联ticket数据,ticket中存储外部工单的信息. ASP 还和claude code深度集成,提供了MCP服务里面包含case查看/更新,alert查看,artifact查看,创建enrichment,playbook查看,运行,运行playbook列表. knowledge更新/搜索,ticket的查看,更新,创建. MCP还集成了SIEM的接口,支持查看当前有那些index,这些index的字段信息,关键字搜索日志,精确匹配搜索,查看对应index的拥有的字段等. siem_explore_schema / siem_discover_index_fields 这两个区别说明一下,siem_explore_schema 是读取存储在ASP中的index字段信息,是包含字段的说明和样例的,这些不用调用SIEM接口,siem_discover_index_fields 是调用SIEM查字段信息,不包含说明. MCP也支持从redis stream读取message的接口. ASP还提供很多skill,包含ASP 的SIRP平台(可以理解为ASP的前端操作平台)的各种用户在UI上的操作. 还有就是指导性的skill,比如指导用户如何创建module ,指导用户如何根据siem_discover_index_fields 返回信息创建siem_explore_schema 需要的yaml(一个index一个yaml),指导用户如何使用MCP中的SIEM接口查询日志等.